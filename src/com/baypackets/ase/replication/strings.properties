# The following string resources are used by the classes in the 
# "com.baypackets.ase.replication" package.

PolicyManagerImpl.setPolicyModeUsage=Usage: set-policy-mode (NORMAL | CONTAINER_ONLY | ALWAYS_REPLICATE | NEVER_REPLICATE)
PolicyManagerImpl.reloadPoliciesUsage=The "reload-policies" command re-loads the container's default replication policies from the backing store.
PolicyManagerImpl.dumpPoliciesUsage=The "dump-policies" command prints a dump of all replication policies.
PolicyManagerImpl.error=The following error occured: {0}
