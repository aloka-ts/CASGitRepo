<?xml version="1.0" encoding="UTF-8"?>
<datasources>
  <local-tx-datasource>
    <jndi-name>PortalDS</jndi-name>
    <connection-url>${genband.maps.connection_url}</connection-url>
    <driver-class>${genband.maps.driver_class}</driver-class>
    <user-name>${genband.maps.db_user}</user-name> 
    <password>${genband.maps.db_passwd}</password>
    <connection-property name="autoReconnect ">true</connection-property> 
    <min-pool-size>${genband.maps.min_pool_size}</min-pool-size>
    <max-pool-size>${genband.maps.max_pool_size}</max-pool-size> 
    <blocking-timeout-millis>5000</blocking-timeout-millis> 
    <idle-timeout-minutes>5</idle-timeout-minutes> 
    <use-fast-fail>True</use-fast-fail> 
    <!--valid-connection-checker-class-name>org.jboss.resource.adapter.jdbc.vendor.OracleValidConnectionChecker</valid-connection-checker-class-name>
    <exception-sorter-class-name>com.baypackets.sms.common.session.DefaultDSOracleExceptionSorter</exception-sorter-class-name-->       
  </local-tx-datasource>
</datasources>
